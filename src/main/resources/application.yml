spring:
  application:
    name: Testing
  datasource:
    url: jdbc:mysql://localhost:3306/do
    password: root
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081
